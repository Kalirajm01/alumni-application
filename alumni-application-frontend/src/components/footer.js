import React from 'react';

const Footer = () => {
  return (
    <footer style={styles.footer}>
    </footer>
  );
};

const styles = {
  footer: {
    backgroundColor: '#0F5CBF',
    padding: '10px',
    position: 'fixed',
    bottom: '0',
    width: '100%',
    height: '165px',
    flexShrink: 0,
  },
  
};

export default Footer;
